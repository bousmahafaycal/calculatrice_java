class RationalNumber extends Number {
	protected int numerator;
	protected int denominator;

	public RationnelNumber(int numerator, int denominator){
		this.numerator = numerator;
		this.denominator = denominator;
	}
}